name: dribble_finance_app_design
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  device_preview: ^1.1.0
  sliding_up_panel: ^2.0.0+1
  flutter:
    sdk: flutter
  flutter_feather_icons: ^2.0.0+1
  http: ^1.2.1
  flutter_bloc: ^8.0.1
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
